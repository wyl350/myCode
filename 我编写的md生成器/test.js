// 如何回调 递归创建文件夹
var
  fs = require('fs'),
function f(...a) {
  if (a.length >= 2) {
    a.forEach(function () {
      
    })
  }

  if (a.length >= 2) {
    fs.mkdir('css', function (error) {
      a[0]
    })


  }
  // a[0]
  // a[a.length-2]

}